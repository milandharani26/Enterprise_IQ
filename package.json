{
  "name": "enterpriseiq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0",
    "prettier": "^3.8.3"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "prettier --write"
    ],
    "enterpriseiq-frontend/**/*.{ts,tsx}": [
      "prettier --write"
    ]
  }
}
