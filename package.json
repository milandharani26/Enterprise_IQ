{
  "name": "enterpriseiq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "prettier --write",
      "eslint --fix"
    ],
    "enterpriseiq-frontend/**/*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}
